{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="container">
    <form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mt-5 mb-3">
            Vous êtes connectés avec l'adresse {{ app.user.username }}.<a href="{{ path('app_logout') }}"> Cliquez pour vous déconnecter</a>
        </div>
    {% endif %}

    <h1 class="h2">Se connecter</h1>

        <div class="row mt-3">
            <div class="col-4">
                <label for="inputEmail">Entrez votre email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col-4">
                <label for="inputPassword">Mot de passe</label>
                <input type="password" name="password" id="inputPassword" class="form-control" required>
            </div>
        </div>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

        <div class="row mt-3">
        <div class="col-4">
            <button class="btn btn-block btn-primary" type="submit" >Se connecter</button>
        </div>
    </div>

    </form>
    </div>

{% endblock %}
